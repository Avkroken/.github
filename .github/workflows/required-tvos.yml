name: tvOS required

on:
  pull_request:
  merge_group:
    types: [checks_requested]

permissions:
  contents: read

jobs:
  bastion:
    name: Bastion tvOS
    if: ${{ github.repository == 'Avkroken/Bastion' }}
    uses: Avkroken/.github/.github/workflows/xcode.yml@main
    with:
      platform: tvos
      runner: macos-26
      package_path: App
      xcodegen_spec: App/project.yml
      generation_script: App/generate-project.sh
      project_path: App/Bastion.xcodeproj
      scheme: Bastion-tvOS
      destination: generic/platform=tvOS Simulator

  unsupported:
    name: Unsupported tvOS repository
    if: ${{ github.repository != 'Avkroken/Bastion' && github.repository != 'Avkroken/.github' }}
    runs-on: ubuntu-latest
    steps:
      - name: Reject missing tvOS CI profile
        shell: bash
        run: |
          echo "::error::No tvOS CI profile is configured for $GITHUB_REPOSITORY"
          exit 1
