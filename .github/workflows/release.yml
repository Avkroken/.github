name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions: {}

jobs:
  release:
    uses: ./.github/workflows/release-please.yml
    secrets:
      app-private-key: ${{ secrets.GAMNACKEN_PRIVATE_KEY }}
