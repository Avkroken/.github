name: Central metadata events

on:
  issues:
    types: [opened, reopened, labeled, unlabeled]
  schedule:
    - cron: '13 * * * *'
  workflow_dispatch:

permissions: {}

jobs:
  metadata:
    permissions:
      issues: write
      pull-requests: write
    uses: ./.github/workflows/metadata-orchestration.yml
